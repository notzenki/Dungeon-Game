extends CollisionShape2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

#func _draw():
	#var rect_size = shape.size # Tamaño del hurtbox
	## var rect_position = self.position - rect_size / 2
	#var rect_position = Vector2(0,0) - rect_size/2
	#var rect = Rect2(rect_position, rect_size)
	#draw_rect(rect, Color(0, 0, 1, 0.5), true)
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#queue_redraw()
